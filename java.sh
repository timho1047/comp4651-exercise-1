# Install JDK (Java Development Kit)
sudo apt update
sudo apt install default-jdk

# use java -version to check if it is installed
java -version


# use javac -version to check if it is installed
javac -version

# Install Maven
sudo apt install maven

# Verify Maven installation
mvn -version

